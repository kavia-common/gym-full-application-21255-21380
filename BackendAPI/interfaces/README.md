This folder will contain the generated OpenAPI specification.

Generate/update the spec by running (from the BackendAPI root):
  python -m src.api.generate_openapi

The output will be written to interfaces/openapi.json
